previewsEnabled: true
databases:
  - name: rails7masterdetaildb
    databaseName: rails7example-master-detail
    user: rails7example
    postgresMajorVersion: 14
    region: frankfurt

services:
  - type: web
    name: rails7-example-master-detail
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    region: frankfurt
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rails7masterdetaildb
          property: connectionString
      - key: SECRET_KEY_BASE
        generateValue: true
